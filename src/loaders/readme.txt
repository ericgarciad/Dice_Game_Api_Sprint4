Poner la carga de los modulos que necesitemos, el arranque de los modulos que necesitemos de express

npm install mysql --save
npm install knex --save (CRUD profesional, crear modelos)
npm install dotenv --save (crear variables de entorno) creamos archiv oculto .env